## Qt project

For Qt project, if not environment is specified, use Qt from the following location: ~/Qt/6.10.1/macos

## Thing that you should not do

- Do NOT perform any git operations (commit, push, pull, merge, rebase, etc.) unless explicitly asked to do so.
