set-environment -g PATH "/opt/homebrew/bin:/bin:/usr/bin"

set -g allow-passthrough on

set -g prefix C-s
unbind C-b

bind % split-window -h -c "#{pane_current_path}"

bind '"' split-window -v -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"

unbind r
bind r source-file ~/.tmux.conf

bind -r j resize-pane -D 5
bind -r k resize-pane -U 5
bind -r l resize-pane -R 5
bind -r h resize-pane -L 5

bind g popup -E -w 90% -h 90% -d "#{pane_current_path}" lazygit

set-option -g history-limit 100000

set -g mouse on
set -g renumber-windows on
set -g status-position top

set-window-option -g mode-keys vi

bind-key -T copy-mode-vi 'v' send -X begin-selection # start selecting text with "v"
bind-key -T copy-mode-vi 'y' send -X copy-selection # copy text with "y"
bind-key -r i run-shell "tmux neww ~/.dotfiles/bin/cht.sh"

unbind -T copy-mode-vi MouseDragEnd1Pane # don't exit copy mode after dragging with mouse

# tpm plugin
set -g @plugin 'tmux-plugins/tpm'

# list of tmux plugins
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator' # for navigating panes and vim/nvim with Ctrl-hjkl
set -g @plugin 'jimeh/tmux-themepack' # to configure tmux theme
set -g @plugin 'tmux-plugins/tmux-resurrect' # persist tmux sessions after computer restart
set -g @plugin 'tmux-plugins/tmux-continuum' # automatically saves sessions for you every 15 minutes
set -g @plugin 'tmux-plugins/tmux-open'

set -g @themepack 'powerline/default/yellow' # use this theme for tmux
#
set -g @resurrect-capture-pane-contents 'on' # allow tmux-ressurect to capture pane contents
set -g @continuum-restore 'on' # enable tmux-continuum functionality
#
# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'

set-option -g status-left-style "#{?client_prefix,fg=colour233#,bg=colour227,fg=colour240#,bg=colour233}"
set-option -g status-style "#{?client_prefix,fg=colour233#,bg=colour227,fg=colour240#,bg=colour233}"
set-option -g status-left "#{?client_prefix,[PREFIX PRESSED],                }"
