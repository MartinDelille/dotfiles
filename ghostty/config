# vim: set filetype=conf:

window-save-state = always
command = "/bin/bash -c '/opt/homebrew/bin/tmux attach || /opt/homebrew/bin/tmux new'"

theme = dark:Catppuccin Frappe, light:One Half Light
custom-shader = cursor_blaze.glsl

